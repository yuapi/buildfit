# Claude Code 세션 이전 가이드

설계 단계를 마치고 구현으로 넘어가기 위한 준비 문서. **레포 생성 → 문서 배치 → 첫 세션 실행** 순서로 따라가면 된다.

---

> **새 세션으로 이어받는다면 §8부터 본다** (2026-09-22). 컨테이너가 새로 뜨면
> DB·원본·적재 데이터가 없다 — 다시 만드는 순서와 이 환경의 함정이 거기 있다.
>
> **이 문서를 지금 어떻게 읽어야 하나** (2026-09-21 기준)
>
> §1~3은 **이미 지난 단계다.** 레포도 코드도 있고, `CLAUDE.md`의 정본은 레포 루트에 있다
> (여기 §2에 복사된 것은 초기 버전이라 낡았다 — 현재 단계·기술 스택은 루트 쪽을 본다).
>
> 아직 쓰이는 절은 둘이다.
>
> - **§4 이슈 순서** — 여기 적힌 "이슈 #n"은 **계획 번호**이고 GitHub 이슈 번호와 다르다.
>   다른 문서가 "이슈 #n"이라고 할 때 그 대응표가 여기 있다
> - **§7 병렬로 직접 할 것** — 도메인·배포 환경변수처럼 **사람만 할 수 있는 일**의 목록
>
> 현재 상태와 돌아가는 화면 목록은 `README.md`를 본다.

---

## 1. 레포 초기 구조

```
pc-builder/                     (이름은 자유)
├── CLAUDE.md                   ★ 세션 컨텍스트
├── README.md
├── docs/
│   ├── pc-builder-spec.md      제품 정의서
│   ├── pipeline-plan.md        인프라 설계 (구현 보류)
│   ├── decisions/              ADR — 왜 그렇게 정했는가
│   │   └── 0001-template.md
│   └── research/               조사 결과
│       └── .gitkeep
└── .github/
    └── ISSUE_TEMPLATE/
        ├── task.md
        └── research.md
```

코드 디렉터리는 만들지 않는다. 첫 이슈가 조사이므로 스택이 정해진 뒤에 세션이 만들게 한다.

---

## 2. CLAUDE.md (그대로 복사해서 수정)

```markdown
# 프로젝트 컨텍스트

## 무엇을 만드는가

PC 견적 검증 도구. 부품 조합의 호환성·전력·전기요금을 판정한다.
가격 비교가 아니라 **판단을 돕는 도구**다. 다나와와 경쟁하지 않는다.

상세: `docs/pc-builder-spec.md`

## 작업 규칙

1. **구현 전에 `docs/pc-builder-spec.md`의 해당 섹션을 먼저 읽는다.**
   추측으로 구현하지 않는다.
2. 스펙과 다르게 구현해야 할 이유가 생기면 **먼저 문서를 고치고 승인을 받는다.**
   코드와 문서가 어긋난 채로 진행하지 않는다.
3. 설계 결정은 `docs/decisions/`에 ADR로 남긴다.
4. 조사 결과는 `docs/research/`에 markdown으로 남긴다.
5. 커밋은 작게. 한 커밋에 한 가지 변경.

## 절대 규칙

- **회원가입·로그인 기능을 만들지 않는다.** 개인정보를 다루지 않는 것이 이 프로젝트의 안전 전제다.
- **localStorage 저장 포맷은 add-only.** 필드 추가는 되지만 의미 변경·제거는 안 된다.
  서버는 롤백되지만 사용자 브라우저는 롤백되지 않는다.
- **성능 수치를 단일 점수로 만들지 않는다.** 용도·해상도 축으로 쪼개서 제시한다.
- **근거 없는 수치를 지어내지 않는다.** 데이터가 없으면 없다고 표시한다.
- 외부 데이터 출처는 `source_url`과 함께 저장한다.

## 현재 단계

Phase 0a: OpenDB 적재 + 어드민 (국내 유통 매칭은 폐기 — ADR-0022)
(로드맵: `docs/pc-builder-spec.md` §11)

## 보류 중

`docs/pipeline-plan.md`는 자율 운영 인프라 설계다.
**지금은 구현하지 않는다.** 앱이 배포 가능한 상태가 된 뒤에 착수한다.
참고용으로만 읽는다.

## 기술 스택

(첫 조사 이슈 완료 후 채운다)
```

---

## 3. 첫 세션 프롬프트

레포 연결 후 아래를 그대로 붙여넣는다.

```
docs/pc-builder-spec.md의 §5.7을 읽고, BuildCores OpenDB 조사를 진행해줘.

작업:
1. github.com/buildcores/buildcores-open-db 를 clone
2. /schemas/ 전체를 분석해서 카테고리별 필드 목록 정리
3. 아래 세 가지를 확인 (§5.7의 "clone 직후 확인" 항목)
   - 케이스 스키마에 GPU 최대 길이 / 쿨러 최대 높이 / 라디에이터 장착 필드가 있는가
   - GPU 스키마에 AIB 모델별 물리 치수(길이, 두께)가 있는가
   - 현행 세대 부품 커버리지 — 샘플 20개를 찾아본다
4. §4.1의 호환성 규칙 8개 각각에 대해, 필요한 필드가 OpenDB에 존재하는지 대조표 작성
5. 결과를 docs/research/opendb-schema-analysis.md 로 저장

결론에는 반드시 포함할 것:
- 규칙 8개 중 OpenDB만으로 구현 가능한 것 / 수동 보강이 필요한 것
- 수동 보강이 필요하다면 그 규모 추정

코드는 작성하지 말고 조사와 문서화만 해줘.
```

---

## 4. 이슈 순서

> **아래 표의 번호는 계획 번호다. GitHub 이슈 번호와 다르다.**
> 다른 문서의 "이슈 #3" 같은 표기는 전부 이 표를 가리킨다.
>
> 2026-09-19 기준 진행: 계획 #1~#4 ✅ · #5 ⛔ · #6~#10 ✅
>
> 남은 일은 GitHub 이슈로 옮겼다. **앞으로 새 작업은 GitHub 이슈로 연다.**
>
> | GitHub | 내용 | 상태 (2026-09-22) |
> |---|---|---|
> | [#1](../../issues/1) | 네이버 쇼핑 API 연동 + 국내 유통 매칭 (계획 #5) | ☒ **폐기** (ADR-0022, 2026-09-23). 쇼핑 API 종료, 대체 소스 없음 |
> | [#2](../../issues/2) | 전기요금 계산 | ✅ **사용자가 한전 화면을 캡처해 주어 풀렸다** |
> | [#3](../../issues/3) | 케이스 데이터 보강 — 판정 불가 83% | ☒ **닫았다** (ADR-0025, 2026-09-25). 수동 입력을 계획하지 않는다 — 업스트림 재동기로만 늘린다 |
> | [#4](../../issues/4) | 규칙 10·11·13·14 — 1차 소스에 필드 없음 | ☒ **폐기** (ADR-0025, 2026-09-25). 자동으로 얻을 길이 없다 |
> | [#5](../../issues/5) | 스토리지 슬롯 | ✅ 호환성(규칙 17·18·19)과 전력(드라이브당 0~15W, 2026-09-24) 모두 끝났다 |
> | [#6](../../issues/6) | 디자인 시스템 + 화면 마감 | ✅ 닫았다 |
> | [#7](../../issues/7) | `/admin` 접근 제어 | ✅ 공유 비밀 하나 + 기본은 꺼짐 (ADR-0020) |
> | [#8](../../issues/8) | 네이버 API 약관 — 결과 저장 금지 조항 | ☒ 저장·캐싱 금지로 확인됐고, 쇼핑 API는 종료됐다 (ADR-0022) |
> | [#9](../../issues/9) | 규칙 15 — GPU 두께 ≤ 케이스 확장 슬롯 | ✅ |
> | [#10](../../issues/10) | 규칙 16 — 총 메모리 용량 ≤ 보드·CPU 최대 | ✅ |
> | [#11](../../issues/11) | 같은 제품이 목록에 여러 번 — 중복 레코드 | ✅ 대표만 보여준다. id는 여전히 열린다 |
> | [#12](../../issues/12) | `/admin`이 값 충돌을 보여주지 않는다 | ✅ 어긋난 값을 나란히 |
> | [#13](../../issues/13) | 판정이 「검증 중」 값을 쓰면서 말하지 않는다 | ✅ ADR-0021 |
> | [#14](../../issues/14) | 고르기가 「검증 중」 값으로 부품을 숨긴다 | ✅ |
>
> **⛔ 표시는 전부 사람이 해야 하는 일이다.** 코드로 풀리지 않는다 — §7을 본다.
>
> **막힌 것이 여전히 막혀 있는지는 원본으로 다시 확인했다** —
> `docs/research/opendb-schema-analysis.md` §10. 같은 질문을 다시 캐지 않는다.

| # | 제목 | 유형 | 선행 |
|---|---|---|---|
| 1 | OpenDB 스키마 조사 | 조사 | — |
| 2 | 기술 스택 결정 + ADR | 결정 | #1 |
| 3 | OpenDB 적재 스크립트 | 구현 | #2 |
| 4 | ~~국내 총판 사전 초안~~ — 폐기 (ADR-0022) | 데이터 | — |
| 5 | ~~네이버 쇼핑 API 연동 + 유통 매칭~~ — 폐기 (ADR-0022) | 구현 | #3 #4 |
| 6 | 어드민 — 빈 필드 보강 | 구현 | #3 |
| 7 | 호환성 규칙 엔진 | 구현 | #3 |
| 8 | 전력 계산 + PSU 권장 | 구현 | #7 |
| 9 | 견적 UI + 결과 화면 | 구현 | #7 #8 |
| 10 | localStorage 저장 + 공유 URL | 구현 | #9 |

**#1과 #2를 건너뛰지 않는다.** 스키마를 모르는 상태에서 적재 스크립트를 짜면 다시 쓰게 된다.

~~#4는 조사와 병렬로 진행 가능하다.~~ #4·#5는 폐기했다 (ADR-0022).

---

## 5. 설계 문서를 세션에서 유지하는 방법

문서가 레포에 있으므로 코드와 함께 버전 관리된다. 이 이점을 살리려면 규칙이 필요하다.

### 변경 흐름

```
설계가 바뀌어야 함
      ↓
docs/ 수정 → 커밋 → 승인
      ↓
그 다음에 구현
```

**코드를 먼저 고치고 문서를 나중에 맞추지 않는다.** 문서가 뒤처지는 순간 다음 세션이 잘못된 컨텍스트로 시작한다.

### ADR 템플릿

`docs/decisions/0001-template.md`

```markdown
# ADR-000X: 제목

## 상태
제안 / 채택 / 폐기 / 대체됨(ADR-000Y)

## 배경
어떤 문제 상황인가

## 결정
무엇을 정했는가

## 이유
왜 그렇게 정했는가. 검토한 대안과 탈락 이유

## 결과
이 결정으로 생기는 제약과 후속 작업
```

### 이미 내려진 결정들 (ADR로 옮길 것)

지금까지 대화에서 정해진 사항. 레포 생성 시 ADR로 기록해두면 이후 세션이 되묻지 않는다.

```
ADR-0001  회원가입을 만들지 않는다
ADR-0002  BuildCores OpenDB를 1차 데이터 소스로 한다
ADR-0003  parts(글로벌)와 skus(국내 유통)를 분리한다  ← 대체됨 (ADR-0022)
ADR-0004  성능 지표를 단일 점수로 만들지 않는다
ADR-0005  localStorage 저장 포맷은 add-only
ADR-0006  가격 비교 커머스로 경쟁하지 않는다
ADR-0007  예측 모델은 1년 이력이 쌓인 뒤 재검토 (기술통계 우선)  ← 대체됨 (ADR-0022)
```

---

## 6. 세션 운영 메모

- **긴 작업은 이슈 단위로 끊는다.** 한 세션에서 여러 이슈를 처리하면 컨텍스트가 흐려진다
- 세션 시작 시 관련 문서 섹션을 명시적으로 지정한다. "spec을 읽어봐"보다 "§5.7을 읽어봐"가 낫다
- 조사 결과가 설계를 바꾸면 **먼저 문서 PR을 올리게 한다**
- 클라우드 세션은 실행 환경이 격리되어 있어 외부 네트워크 접근이 제한될 수 있다. 네이버 API 연동(#5)은 로컬이나 서버에서 확인이 필요할 수 있다

---

## 7. 병렬로 직접 할 것

Claude에게 넘기지 않는 작업.

```
□ 서버 OS 설치 및 기본 보안
□ 네트워크 구성, 방화벽  ← pipeline-plan.md §14.4.3.1
□ k3s 설치
□ 도메인 확보  ← 이게 없으면 배포 확인 항목 하나를 볼 수 없다 (deployment.md §2)
□ 배포 환경변수에 ADMIN_TOKEN 넣기  ← ADR-0020. 없으면 /admin이 404다
□ ★ 빌드 명령에 NEXT_PUBLIC_SITE_URL 넣기  ← 런타임에만 넣으면
                                             localhost:3000이 색인에 굳는다.
                                             오류가 나지 않는다 (deployment.md §2)
□ 배포 플랫폼의 헬스체크를 /healthz로  ← 공개 화면은 DB가 끊겨도 200이다
□ git remote를 새 레포 이름으로  ← `git remote set-url origin <새 주소>`
                                   지금은 GitHub 리다이렉트로 동작한다
```

배포 절차와 확인 항목은 `docs/deployment.md`에 있다. **프로덕션 빌드로 실제
확인한 것만** 적혀 있다.

**방화벽 설정은 반드시 직접 한다.** 공인 IP에 6443이 열리는 사고는 한 번이면 끝이고, 지금은 브로커도 워치독도 없어 자율 운영의 전제 장치가 하나도 없는 상태다.

---

## 8. 새 세션으로 이어받기 (2026-09-22)

클라우드 세션은 **컨테이너가 새로 뜬다.** 코드는 git으로 넘어가지만, 아래 것들은
git에 없어서 새 세션에서 다시 만들어야 한다. 이 절을 빠뜨리면 새 세션은 빈 DB
위에서 시작하고, DB에 기대는 테스트가 전부 건너뛰거나 깨진다.

### 8.1 넘길 때의 상태

| | |
|---|---|
| 코드 | `develop` = `origin/develop` (2026-09-24). 작업 트리 깨끗. 원격 브랜치는 `main`·`develop`·세션 브랜치 셋뿐이다 |
| 머지 | **PR로만 된다** (ADR-0024). `main`·`develop`은 Ruleset으로 보호된다 — 직접 push 거절, `verify` 녹색이어야 머지, merge 방식만. 머지된 `feature/`는 워크플로가 지운다 |
| CI | PR마다 `verify`가 두 번 돈다(push·pull_request). `develop`의 모든 머지 커밋도 돈다 |
| 열린 이슈 | 없다. #3(케이스 데이터 수동 입력)·#4(원본에 필드 없음)는 2026-09-25 **사용 불가로 판단해 닫았다** (ADR-0025). #1·#8은 2026-09-23 폐기로 닫았다 (ADR-0022) |
| 닫은 이슈 (이번 흐름) | #11~#16 (중복·검증 중·소켓) · #17~#20 (규칙 20·자기 검사) · #23~#25 (규칙 21·22·23) · #26 스토리지 부품 페이지 500 · #27 규칙 17 드라이브 1개 · #28 규칙 12 소켓 하한 · #29 규칙 8 거짓 오류 · #30 쿨러 전력 · #31 렌더링 측정값 · #32 규칙 24 · #33 규칙 17 M.2 0 · #34 규칙 20 sTR5 · #35 규칙 18 적용 순서 · #36 PR 머지 흐름 · #39 액션 Node 24 · #44 규칙 7 여유분 문구 · #46 견적 화면 문구 · #48 부품 페이지 출시 연도 · #50 JSX 조사 · #52 측정값 포장 표기 · #54·#56 비교 후보 축 · #58 결측 현황 중복 |

**`main`은 비어 있다** (`develop`보다 316 커밋 뒤). 배포한 것이 없어서다 —
첫 배포 절차는 `docs/deployment.md` §6.

### 8.2 git에 없어서 다시 만들어야 하는 것

| 것 | 어디서 | 없으면 |
|---|---|---|
| PostgreSQL 역할·DB | 컨테이너 | 모든 DB 테스트가 건너뜀 / 앱이 「불러올 수 없습니다」 |
| `.env` | `.env.example` 복사 | 스크립트가 `DATABASE_URL`을 못 찾음 |
| OpenDB clone (295MB) | GitHub | 적재 불가 |
| 적재된 카탈로그 (26,485건) | 적재 22초 | `/rules` 수치가 0, `sample`·`measure:quote`가 무의미 |

### 8.3 다시 만드는 순서

> **이제 자동이다 (2026-09-22).** `.claude/hooks/session-start.sh`가 새 클라우드
> 세션이 뜰 때 아래를 전부 한다 — 의존성, PostgreSQL 역할·DB, 마이그레이션, 원본
> clone, 적재. 그리고 `DATABASE_URL`을 세션에 넘긴다. 이미 있으면 건너뛰므로
> 캐시된 컨테이너에서는 3초 남짓이고, 처음 뜨는 컨테이너에서는 적재까지 40초 남짓이다.
>
> 원본 clone이나 적재가 실패해도 **세션은 뜬다** (경고만 남긴다). 아래 명령은 훅이
> 실패했을 때, 또는 로컬 머신에서 손으로 할 때 쓴다.

**그대로 붙여넣는다.** 이 컨테이너에서 쓰던 설정과 같다 (역할 이름·비밀번호는
로컬 전용이고 비밀이 아니다 — ADR-0001, `docker-compose.yml` 주석).

```bash
# 1) PostgreSQL 16. 이미 떠 있으면 무시된다
pg_ctlcluster 16 main start
pg_isready

# 2) 역할과 DB. 슈퍼유저로 만든다 — 테스트가 격리 DB를 만들고 지우며
#    (`create database` / `drop … with (force)`), 마이그레이션이 pg_trgm 확장을 만든다
su postgres -c "psql -c \"create role buildfit login superuser password 'buildfit'\""
su postgres -c "createdb -O buildfit buildfit"

# 3) 설정
cp .env.example .env
#   DATABASE_URL=postgres://buildfit:buildfit@localhost:5432/buildfit
#   OPENDB_PATH=../buildcores-open-db

# 4) 의존성과 스키마
npm ci
DATABASE_URL=postgres://buildfit:buildfit@localhost:5432/buildfit npm run migrate

# 5) 원본과 적재 (레포 바깥에 clone한다)
git clone --depth 1 https://github.com/buildcores/buildcores-open-db ../buildcores-open-db
DATABASE_URL=postgres://buildfit:buildfit@localhost:5432/buildfit \
  OPENDB_PATH=../buildcores-open-db npm run ingest

# 6) (선택) 성능 측정값 — ADR-0023. 빼면 부품 페이지가 「측정값 없음」을 적는다
#    SessionStart 훅은 이 단계를 하지 않는다 (압축 101MB)
curl -sSO https://opendata.blender.org/snapshots/opendata-latest.zip
npm run ingest:benchmarks -- opendata-latest.zip
```

적재 끝 로그가 다음과 **비슷하면** 된다. 원본이 매일 동기화되므로 건수는
조금씩 다를 수 있다 (넘길 때 원본은 `e90bff27`, 2026-09-21 동기화분).

```
중복 레코드 묶는 중...
  499그룹 · 537건을 대표 아님으로 표시
  값이 어긋나는 스펙 274건에 검증 표시
완료 (22s)
  parts        26485
```

### 8.4 다 됐는지 확인

```bash
export DATABASE_URL=postgres://buildfit:buildfit@localhost:5432/buildfit
npm run typecheck && npm run lint
npm test                 # 적재된 DB로 — 39개 파일
npm run test:empty-db    # ★ CI와 같은 조건(빈 DB)으로 — 로컬만 통과하는 것을 잡는다
npm run sample -w @buildfit/ingest   # 규칙별 판정 불가율. 조사 §9.3.2와 비슷해야 한다
```

**`test:empty-db`를 건너뛰지 않는다.** 로컬에는 카탈로그가 있고 CI에는 없다.
이 차이로 CI를 두 번 깨뜨렸다 (run 57·58).

### 8.5 이 환경의 함정

넘기기 전 세션에서 실제로 걸린 것들이다. 하나씩 시간을 잃었다.

- **PostgreSQL이 도구 호출 사이에 죽는다.** 테스트가 무더기로 실패하면 코드보다
  먼저 `pg_isready`를 본다. `no response`면 `pg_ctlcluster 16 main start`.
  로그에 "end-of-recovery"가 남는다 — 깨끗한 종료가 아니다
- **`pkill -f next-server`가 자기 셸을 죽인다.** 패턴이 명령줄 자체와 맞는다.
  `pkill -9 -f 'next-serve[r]'`로 쓴다
- **포트 3100에 낡은 서버가 남는다.** 새로 빌드했는데 동작이 그대로면 옛 서버가
  응답하고 있는 것이다. 위 명령으로 죽이고 포트가 비었는지 보고 띄운다
- **`git remote`가 아직 옛 이름을 가리킨다.** GitHub 리다이렉트로 push는 된다
  (`remote: This repository moved` 경고가 뜬다). 고치는 것은 사람 몫이다 (§7)
- **`develop`에 push하면 거절된다** (2026-09-24부터, Ruleset). 로컬에서 `git merge --no-ff` 후
  push하던 옛 절차는 막힌다. feature 브랜치를 push하고 GitHub MCP로 PR을 열어
  `merge_pull_request`(`merge_method: merge`)로 머지한다. CI가 끝나기 전 머지는
  「Required status check "verify" is in progress」로 거절된다 — 기다린다
- **CI를 기다릴 때 `sleep`을 전경에서 쓰지 않는다.** 막힌다. `run_in_background`로
  `sleep 90`을 걸면 끝날 때 알림이 온다. 한 번 도는 데 1분 20초 남짓이다
- **`git push --delete`는 403이다** (프록시). 원격 브랜치는 워크플로가 지운다 —
  머지 때 `delete-merged-branch.yml`, 남은 것은 `prune-merged-branches.yml`(손으로, 기본 dry run)
- **세션 훅은 원본을 새로 받지 않는다.** 카탈로그가 있으면 적재도 건너뛴다. 업스트림이 채운 값을
  받으려면 `OPENDB_PATH=… npm run sync:opendb` (원본 pull → 새 커밋이 있으면 재적재 + id 확인, 1분 남짓)
- **이 세션의 GitHub 도구는 옛 레포 이름(`partfit`)으로 부른다.** `buildfit`으로 부르면
  「설정되지 않은 레포」로 거절된다. 레포 API를 `curl`로 직접 부르는 것도 프록시가 막는다
- **SQL 템플릿 안에 백틱을 쓰지 않는다.** drizzle의 `` sql`…` ``은 템플릿
  리터럴이라 SQL 주석 속 백틱이 문자열을 끊는다. 두 번 걸렸다
- **drizzle은 select 목록 안의 컬럼을 테이블 없이 적는다.** 상관 서브쿼리를
  select 목록에 쓰면 별칭에 걸려 조용히 `null`이 된다. `alias()` + `leftJoin`을 쓴다
- **문서만 고친 커밋도 테스트를 돌린다.** `repo-name.test.ts`가 문서 속 레포
  주소를 잡는다. 문서라고 건너뛰었다가 CI를 깨뜨렸다 (run 56)

#### 스크래치 스크립트와 브라우저

- 레포 밖에서 `tsx`로 `@buildfit/*`를 import하려면 그 폴더에 레포의
  `node_modules`를 심볼릭 링크한다: `ln -s <레포>/node_modules <폴더>/node_modules`
- Chromium이 깔려 있다. `playwright-core`를 스크래치 폴더에 설치하고
  `executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'`로 띄운다.
  `playwright install`은 하지 않는다
- 어드민 화면을 브라우저로 볼 때: 서버를 `ADMIN_TOKEN=<16자 이상>`으로 띄우고
  `httpCredentials: { username: 'x', password: <그 값> }`

### 8.6 새 세션 첫 프롬프트

```
CLAUDE.md와 docs/handoff-guide.md §8을 읽고, §8.3대로 환경을 다시 만든 뒤
§8.4로 확인해줘. 확인이 끝나면 결과만 짧게 보고해.
그다음 CLAUDE.md의 「다음 개선 지점」부터 이어서 진행해.
```

### 8.7 이어서 할 만한 것

막히지 않은 것만 적는다. 자동으로 얻을 수 없는 데이터에 기대는 것은 적지 않는다 (ADR-0025).

- **고르기의 「숨긴 건수」 비용이 제약 개수에 비례한다** (ADR-0016 재측정).
  제약 2개에 짧은 접두사 31ms. 새 규칙에 좁히기 제약을 더할 때 같이 본다
- **GPU 라디에이터 `0`을 「0 mm」로 보인다** — 공랭 117개가 0이다(없다는 뜻). 비교 페이지에서
  「정보 없음 vs 0 mm」가 「다름」으로 뜬다. 표시만의 문제라 두었다
- **파워 부품 페이지가 커넥터 `0`을 그대로 보인다.** 견적 화면(규칙 8)은 8핀·16핀이 둘 다 0이면
  「데이터 이상」으로 보는데, 부품 하나만 보고는 모순이라고 할 수 없다(커넥터 없는 저출력 파워가 있다)
- **화면을 모바일 폭으로 직접 열어 읽는 것이 문구 문제를 찾는 가장 빠른 길이었다** (2026-09-25).
  「일곱 칸」·「2023년인데 연도 비어 있음」·「9600와」·「750W에 1600W 비교」가 전부 그렇게 나왔다.
  테스트는 통과하고 있었다 — 손으로 적은 수와 조사는 테스트가 따로 있어야 잡힌다

한 것 (§8을 쓴 뒤):
- ~~`partsMatchingSpec`의 「검증 중」 값~~ — 재 보니 **2행**(TR4/sTR4 쌍둥이)뿐이었고,
  그 둘은 #16으로 같은 소켓이 되어 검증 표시 자체가 거둬졌다
- `/calc/power` 단독 계산기 (명세 §8)
- SessionStart 훅 (§8.3이 자동이 되었다)
- 규칙 20 — 쿨러가 CPU 소켓을 지원하는가 (이슈 #17). 잘린 `LGA 115`는 이 규칙 안에서만 푼다
- 보간한 값 뒤 조사 12곳 + 가드 (`korean-particle.test.ts`)
- CI: 통합 브랜치 커밋이 대기열에서 밀려나 검증 없이 남던 것 (run #74). 그룹에 SHA를 붙였다
- 소켓보다 앞선 출시 연도에 「검증 중」 (이슈 #18). 5건 — Ryzen 5 7500F가 2020년이었다.
  값은 사람이 `/admin`에서 출처와 함께 채운다
- 재적재가 어드민이 고친 스펙 값을 OpenDB 값으로 되돌리던 것 (이슈 #19). 어긋난 값
  고치기(#12)가 전부 이 경우였다 — `apps/ingest/test/human-values.test.ts`가 지킨다
- 스스로 모순인 메모리 9건에 「검증 중」 (이슈 #20). 규칙 3·16의 입력이다
- 규칙 21·22·23 — 화면 출력·CPU 쿨러·노트북용 메모리 (이슈 #23~#25)
- **국내 유통·가격 폐기** (ADR-0022, 2026-09-23). 소스가 전부 막혔다 — 조사 세 편
  (`naver-shopping-api-status.md` · `domestic-availability-sources.md` · `price-comparison-data-flow.md`).
  `skus`·`importers`는 0행인 채로 지웠다 (마이그레이션 0005, 행이 있으면 멈춘다)
- 스토리지 부품 페이지 3,439쪽이 전부 500이던 것 (이슈 #26). **sitemap 전수 크롤링으로 찾았다** —
  25,684쪽이 전부 200인 것을 확인했다. 새 슬롯·페이지를 더하면 한 번 돌릴 만하다
- 규칙 17 — 드라이브 1개는 슬롯 행의 내용으로 판정 (이슈 #27). 19.9% → 91.9%
- 규칙 12 — 보드 연도가 없으면 소켓의 첫 CPU 연도가 하한 (이슈 #28). 같은 소켓 조합 판정 불가 83.2% → 67.8%
- 접근성 (2026-09-24, axe + 키보드): `<main>` 랜드마크와 「본문으로 건너뛰기」, 스펙 입력칸·제보 폼의
  이름표 연결, **누른 버튼이 사라질 때 초점이 페이지 맨 위로 떨어지던 것** 여섯 군데(고르기 선택·Esc·
  제거, 붙여넣기 열기·넣기, 저장 목록 삭제). 사용자 화면·어드민 모두 axe 위반 0.
  **브라우저 점검은 CI에서 돌지 않는다** — 소스 가드는 `landmarks.test.ts`·`spec-field.test.ts`

**막히지 않은 일은 여기까지다 (2026-09-24).** 명세 §4의 규칙 중 막히지 않은 것은 전부
돌고(1~9, 12, 15~24), §8의 페이지 중 Phase 1까지의 것은 전부 있다. Phase 3은 부품별 렌더링
측정값까지이고 게임 FPS에 기대는 나머지는 폐기했다 (ADR-0025, 2026-09-25). 자동으로 얻을 수 없는
데이터는 사용 불가로 보므로 **사람이 채우기를 기다리는 이슈는 없다.** 남은 것은 아래의 사람 몫이다.

- **첫 배포와 `v0.3.0` 태그.** Phase 1의 완료 기준(누진세 계산 검증)을 채웠다. 다만
  `main`은 「배포된 것만」이라 배포 없이 태그를 붙이지 않는다 (`docs/deployment.md` §6)
- **도메인** — 정해져야 배포 확인 항목 하나를 볼 수 있다
- **`git remote`를 새 레포 이름으로** — 지금은 리다이렉트로 동작한다

# 프로젝트 컨텍스트

## 무엇을 만드는가

PC 견적 검증 도구. 부품 조합의 호환성·전력·전기요금을 판정한다.
가격 비교가 아니라 **판단을 돕는 도구**다. 다나와와 경쟁하지 않는다.

**이름은 `buildfit`이다** (ADR-0019). npm 스코프·DB·화면 로고·레포가 전부 그것이고,
부품 하나가 아니라 **조합**을 판정하는 도구이기 때문이다.
**레포 이름은 코드 어디에도 넣지 않는다** — 그래서 2026-09-22의 개명이 코드 변경
0건으로 끝났다. 그 상태를 유지한다 (`apps/web/test/repo-name.test.ts`가 지킨다).

상세: `docs/pc-builder-spec.md`

**새 세션이면 먼저 `docs/handoff-guide.md` §8을 본다.** 클라우드 세션에서는
`.claude/hooks/session-start.sh`가 DB·원본·적재를 자동으로 세우고 `DATABASE_URL`을
넘긴다. 훅이 실패했을 때의 수동 절차와 이 환경의 함정이 §8에 있다.

## 작업 규칙

1. **구현 전에 `docs/pc-builder-spec.md`의 해당 섹션을 먼저 읽는다.**
   추측으로 구현하지 않는다.
2. 스펙과 다르게 구현해야 할 이유가 생기면 **먼저 문서를 고치고 승인을 받는다.**
   코드와 문서가 어긋난 채로 진행하지 않는다.
3. 설계 결정은 `docs/decisions/`에 ADR로 남긴다.
4. 조사 결과는 `docs/research/`에 markdown으로 남긴다.
5. 커밋은 작게. 한 커밋에 한 가지 변경.

## 브랜치 전략

Git Flow를 따른다. 근거와 상세: `docs/decisions/0008-git-flow-branching.md`

| 브랜치 | 용도 | 분기 원본 | 머지 대상 |
|---|---|---|---|
| `main` | **배포된 것만.** 릴리스마다 태그 | — | — |
| `develop` | 통합 브랜치. 기본 브랜치 | `main` | — |
| `feature/<이슈번호>-<요약>` | 기능·문서·데이터 작업 | `develop` | `develop` |
| `release/<버전>` | 릴리스 준비 (버그 수정만) | `develop` | `main` + `develop` |
| `hotfix/<요약>` | 배포된 것의 긴급 수정 | `main` | `main` + `develop` |

- **`main`과 `develop`에 직접 커밋하지도, 직접 push하지도 않는다.** 항상 브랜치를 따서 **PR로** 머지한다 (ADR-0024)
- 머지 방식은 **`merge`** — `--no-ff`와 같은 머지 커밋이다. squash·rebase는 작업 단위를 지운다
- **PR의 CI가 녹색일 때 머지한다.** 로컬 머지 후 push하면 CI가 통합 브랜치에 들어간 **뒤에** 돈다
- **이슈 하나 = feature 브랜치 하나.** 여러 이슈를 한 브랜치에서 처리하지 않는다
- 머지한 feature 브랜치는 **워크플로가 지운다** (`.github/workflows/delete-merged-branch.yml`).
  `release/`·`hotfix/`는 두 곳에 머지해야 해서 지우지 않는다 — 레포 설정의 자동 삭제를 쓰지 않는 이유다
- 이슈 번호가 없는 작업은 `feature/<요약>`
- 릴리스 태그는 semver. 1.0 이전이므로 **Phase 완료마다 minor를 올린다**
  (Phase 0a → `v0.1.0`, Phase 0b → `v0.2.0`, …)

```bash
# 작업 시작
git checkout develop && git pull
git checkout -b feature/1-opendb-schema-research

# 작업 종료 — PR로 머지한다 (ADR-0024)
git push -u origin feature/1-opendb-schema-research
# develop으로 PR → CI 녹색 → merge 방식으로 머지 → 워크플로가 원격 브랜치를 지운다
git checkout develop && git pull
git branch -d feature/1-opendb-schema-research   # 로컬 사본만
```

## 절대 규칙

- **회원가입·로그인 기능을 만들지 않는다.** 개인정보를 다루지 않는 것이 이 프로젝트의 안전 전제다.
  어드민은 계정이 아니라 **공유 비밀 하나**로 막는다 (ADR-0020) — 사용자 계정이 아니므로 이 규칙과 어긋나지 않는다.
- **localStorage 저장 포맷은 add-only.** 필드 추가는 되지만 의미 변경·제거는 안 된다.
  서버는 롤백되지만 사용자 브라우저는 롤백되지 않는다.
- **성능 수치를 단일 점수로 만들지 않는다.** 용도·해상도 축으로 쪼개서 제시한다.
- **근거 없는 수치를 지어내지 않는다.** 데이터가 없으면 없다고 표시한다.
- 외부 데이터 출처는 `source_url`과 함께 저장한다.

## 현재 단계

**Phase 0a — 완료**
- OpenDB 적재 ✅ 부품 26,485건 (`apps/ingest`)
- 어드민(빈 필드 보강) ✅ (`/admin`)
- 국내 유통 매칭 ☒ **폐기** (ADR-0022, 이슈 #1). 네이버 쇼핑 API가 2026-07-31에 종료됐고
  대체 소스가 전부 막혔다 — 11번가는 판매자 회원만, 쿠팡은 시간당 10회, 다나와는 소멸.
  가격비교 사이트도 공개 API가 아니라 **쇼핑몰의 입점 계약(EP 피드)** 으로 받는다
  (`docs/research/price-comparison-data-flow.md`)
  - **가격과, 가격이 있어야 성립하는 기능도 함께 폐기했다** — Phase 2(가격·정규화)·4·5,
    총소유비용, 차액 사용처 제안. **Phase 번호는 다시 매기지 않는다**
  - **긁어 오지 않는다.** 크롤링은 데이터베이스제작자 권리·부정경쟁방지법 위험을 들인다
  - 되살리려면 새 ADR로 한다. 소스의 약관(저장·캐싱)을 **설계 전에** 읽는다

**Phase 0b — 완료**
- 호환성 규칙 8개 동작 ✅ (`packages/compat`)
- 견적 구성 도구 ✅ (`/`)
- 공유 링크 ✅ (`/build/[code]`, ADR-0012)
- 로컬 저장 ✅ (§8A, ADR-0005)

**Phase 1 — 진행 중**
- CPU 쿨러 슬롯 + 규칙 9 (높이 ≤ 케이스 한계) ✅ 공유 코드 v2 (ADR-0012)
- 규칙 12 (BIOS 업데이트 필요 여부) ✅ Flashback으로 경고/정보를 가른다
- 고를 때 안 맞는 후보 거르기 ✅ (ADR-0016)
- 한국어 검색 ✅ (ADR-0017) — 라이젠·지포스·삼성이 전부 0건이었다
- 견적서 붙여넣기 ✅ (ADR-0018) — 중복 제외 후 줄의 72.6%가 하나로 확정된다.
  **그 수치는 상한이다** — 출발점이 카탈로그 전체 이름이라 그 자체로 SKU를
  가리킨다. 실제 견적서 줄은 짧아서 계열 여럿을 가리킨다 (조사 §2.1).
  못 찾는 것은 0이다 — 남는 일은 짧은 목록에서 고르기다
- `/admin` 접근 제어 ✅ (ADR-0020) — 공유 비밀 하나. `ADMIN_TOKEN`이 없으면 배포본에서 404
- 규칙 15 (GPU 두께 ≤ 케이스 확장 슬롯) ✅ 판정 불가 3.0%. 규칙 4가 통과시키던 199,203 조합을 잡는다
- 규칙 16 (총 메모리 용량 ≤ 보드·CPU 최대) ✅ 판정 불가 0.3%. 경고 등급
- 스토리지 ✅ 규칙 17·18·19 + 공유 코드 v3. 전력은 드라이브당 **0~15W** (이슈 #5, 2026-09-24).
  Seasonic 원문을 **웹 아카이브 사본**에서 확인했다 — 규칙 7의 「원문 미확인」도 뗐다. 원문은 상한만 주어 하한은 0
  - CPU 쿨러 전력도 같은 원문으로 넣었다 (이슈 #30) — 수랭 15~30W, 공랭 팬 개당 1~3W(조명이 있거나
    모르면 1~6W). **팬 수가 86% 비어 있어도 수를 지어내지 않는다** — 모르면 넣지 않고 그렇다고 적는다
  - 규칙 17은 **개수를 세지 않는다.** `m2_slots` 배열 길이가 슬롯 수가 아니다
    (같은 보드 계열이 2·4·6행으로 존재). 「있다/없다」만 본다 — compat-rules §17.4
  - **드라이브가 1개면 행의 내용으로 판정한다** (이슈 #27, §17.5). 개수는 못 믿어도 행
    하나하나는 실제 슬롯이다. 받는 `길이/방식`이 있으면 통과 — 판정 가능 19.9% → 91.9%.
    적재가 `m2_accepts`를 **집합**으로 담는다 — 행을 담으면 4행·2행 기록이 중복 불일치가 된다
  - **M.2 0개를 무조건 믿지 않는다** (이슈 #33, §17.6). X570 Taichi·ProArt Z490-CREATOR가 0으로
    들어 있어 NVMe에 거짓 오류가 났다. PCIe 목록까지 비었으면 안 적은 레코드라 판정 불가 (17건)
  - **볼 것이 없으면 보드 값을 묻지 않는다** (이슈 #35, §18.3). 규칙 18이 SATA 드라이브가 없어도
    보드 포트 수 결측으로 판정 불가를 내 NVMe만 담은 견적의 27.1%가 「모른다」였다 → 0.1%
- 전기요금 ✅ 누진 구간 반영 (이슈 #2). **사용자가 한전 화면을 캡처해 주어 풀렸다** —
  이 환경은 한전 도메인이 막혀 있다. 표·출처: `docs/research/kepco-tariff.md`
  - 같은 PC가 가구 100kWh 집에서 2,693원, 380kWh 집에서 10,742원이다. **PC 전력만
    따로 곱하면 의미가 없다**는 §2.1의 전제가 실제로 그렇다
  - 기후환경요금·연료비조정요금·부가세·전력산업기반기금은 빠져 있고, 빠졌다고 적는다
- 중복 레코드 묶기 ✅ (이슈 #11) 499그룹 537건(2.03%)이 목록에서 빠진다.
  **합치지 않고 `duplicate_of`로 가리킨다** — id를 없애면 공유 링크가 깨진다
  - 목록·고르기·sitemap·견적서는 대표만 본다. **id·slug로는 여전히 열리고**
    `rel=canonical`이 대표를 가리킨다
  - 그룹 안에서 값이 어긋나는 스펙에 `disputed` (272행). **추측 없이 얻는
    검증 신호다** — 같은 제품인데 값이 다르면 하나는 틀렸다. 규칙 8의 입력 3개 포함
  - 적재가 **자기가 세운 것 중 더는 어긋나지 않는 것을 거둔다.** 사람이 신고한
    것(열린 `spec_reports`가 있는 것)은 거두지 않는다. 비교는 소켓 등가 표를 따른다
  - `/admin`이 그 목록을 어긋난 값과 **나란히** 보여준다 (이슈 #12). 어느 쪽이
    맞는지는 고르지 않는다 — 다수결도 최신순도 근거가 아니다
- 검증 중인 값으로 판정한 사실 표시 ✅ (이슈 #13, ADR-0021). **판정은 바꾸지
  않는다** — `unknown`으로 내리면 신고 하나가 판정을 지우고, 신고가 무기가 된다
  - 규칙 14개는 건드리지 않았다. `SPEC_REQUIREMENTS`로 `evaluate()`가 한 곳에서
    붙인다 — 규칙에 손대면 하나를 빠뜨리고, 빠진 규칙은 조용히 확정 판정을 낸다
- 소켓 표기 등가 ✅ (이슈 #16) — `TR4`/`sTR4`가 같은 소켓인데 문자열로 비교해
  Threadripper+X399 230조합이 전부 오류였다. **확실한 것만 묶는다** — `LGA 2011-3
  Narrow`·듀얼 소켓·`LGA 1151v2`는 칩셋·세대가 받는 CPU를 갈라 묶으면 거짓 통과가
  된다. 표는 `packages/compat/src/sockets.ts` 하나, 규칙 1과 고르기가 같이 쓴다
- 규칙 20 (쿨러가 CPU 소켓을 지원하는가) ✅ (이슈 #17) — 판정 가능 99.9%, 경고 등급.
  AM5 27% · LGA 1700 28% · LGA 1851 33%가 「목록에 없음」이다. **앞 세대로 묶지 않는다** —
  AM4만 적은 쿨러가 AM5에 맞는다고 알려져 있지만 데이터로 증명되지 않는다.
  쿨러의 잘린 `LGA 115`(=115x)는 **규칙 20 안에서만** 푼다 — `sockets.ts`에 넣으면 규칙 1이 거짓 통과한다
  - **소켓 제조사의 공식 문서가 있는 호환만 더한다** (§20.3, 2026-09-24). LGA 1700 쿨러 → LGA 1851은
    Intel 지원 문서가 밝혀 통과(119개). AM4 → AM5는 AMD가 「맞는다」고 했지만 자체 백플레이트
    예외가 있어 **경고 그대로**, 무엇을 확인할지만 덧붙인다. sTR5도 같다 (이슈 #34) — AMD가 밝힌 것은
    「Asetek 호환 쿨러」뿐이다. LGA 2011-3·FM2+·AM3+는 목록에 없음이 98~99%지만 공식 문장을 못 찾아 두었다 (§20.3.1)
- 소켓보다 앞선 출시 연도 표시 ✅ (이슈 #18) — Ryzen 5 7500F(AM5)가 2020년으로
  들어 있어 규칙 12가 그 값으로 판정했다. **고치지도 판정을 바꾸지도 않고** `disputed`만
  세운다. 빈 해를 사이에 두고 앞에 홀로 떨어진 무리만 고른다 — 지금 5건
  (`docs/research/release-year-plausibility.md`)
- 스스로 모순인 메모리 표시 ✅ (이슈 #20) — 모듈 수 × 모듈 용량 ≠ 총 용량이거나 이름의
  `(2x16GB)`와 다른 9건. 경계값이 없는 신호다. 적재의 자기 검사는 `selfCheckFlagsSelect`
  하나에 모은다 — 중복 불일치 검사의 거두기가 그것을 뺀다
- 재적재가 어드민이 고친 스펙 값을 덮던 것 ✅ (이슈 #19) — upsert가 출처를 보지 않았다
- 규칙 21 (화면이 나오는가) ✅ (이슈 #23) — 내장그래픽 없는 CPU(F 모델)에 그래픽카드를
  안 고르면 경고. **고르지 않은 부품을 보는 첫 규칙이다** — `RULE_PARTS`에 CPU만 있다.
  「없음」 표기가 `"None"`·`"0"` 둘이라 `hasIntegratedGraphics` 하나로 판단한다
- 규칙 22 (CPU 쿨러가 있는가) ✅ (이슈 #24) — 기본 쿨러 없는 CPU에 쿨러를 안 고르면 경고.
  21과 같은 구조다. 기본 쿨러가 충분한지는 말하지 않는다 — 근거가 없다
- 규칙 23 (노트북용 메모리가 아닌가) ✅ (이슈 #25) — 규칙 2는 DDR 규격만 봐서 SO-DIMM
  키트를 통과시켰다. 142건(3.1%). 보드에 슬롯 형태 필드가 없어 경고이고, Thin Mini-ITX는
  판정하지 않는다
- 규칙 8 거짓 오류 ✅ (이슈 #29) — **파워 쪽 `0`을 믿고 있었다.** 750W 이상의 약 26%가
  8핀·16핀을 둘 다 0으로 적는다(ATX 3.x 이름 126개 중 56개) → 판정 불가. 16핀만 모자라고
  8핀이 남으면 변환 케이블 경우라 **경고**. 무작위 견적 3,000개에서 문제 45.5% → 23.0%.
  **무작위 견적을 돌려 규칙별 비율을 보는 것이 이런 데이터 문제를 찾는 가장 빠른 길이었다**
- 규칙 24 (PSU 길이 ≤ 케이스 한계) ✅ (이슈 #32) — 경고. 케이스 쪽이 14%만 차 있지만 판정되는
  169만 조합의 5.7%가 넘는다. **보드 영상 출력 검사는 만들지 않았다** — 후면 단자 목록이 부분적이라
  「없다」를 증명할 수 없다 (compat-rules §21.4·§24.2)
- 규칙 10·11·13·14 보류 — OpenDB 스키마에 필드 없음 (이슈 #4)

**Phase 3 — 첫 단계만** (ADR-0023, 이슈 #31)
- 부품·비교 페이지에 **3D 렌더링 측정값** ✅ — Blender Open Data(**CC0**) 4.5의 장치별 중앙값.
  GPU는 실행이 가장 많은 연산 방식, 실행 5회 이상만. 이름 **정확 일치만** 잇고 측정 장치 이름을
  보인다. 부품 3,105개(장치 257개)에 붙었다 — `docs/research/blender-open-data.md`. 포장 표기(OEM/Tray·Box)는
  걷어낸다 — 붙은 채로는 포장 표기 CPU 200개가 하나도 안 맞았다 (이슈 #52)
  - **노트북 GPU는 잇지 않는다** — 「Laptop GPU」를 걷어내면 데스크톱 카드에 붙는다
  - 차이는 ADR-0004의 **체감 언어**로. CPU 값과 GPU 값은 같은 눈금이 아니다
  - 적재는 선택 단계다 (`npm run ingest:benchmarks -- <zip>`). 스냅숏은 레포에 넣지 않는다
- 게임 FPS 시드는 여전히 없다 — 병목·견적 비교(§6.2·§6.3)는 막혀 있다

**규칙은 이제 20개가 돈다** (1~9, 12, 15~24 — 9 + 1 + 10). 번호는 건너뛴 자리를 메우지 않는다 —
명세 §4.2의 번호와 어긋나면 어느 쪽이 맞는지 매번 확인해야 한다.

**남은 작업은 GitHub 이슈에 있다.** 문서의 "이슈 #n"은 handoff-guide §4의
계획 번호이고 GitHub 번호와 다르다. 대응표는 그 절에 있다.

로드맵: `docs/pc-builder-spec.md` §11

**다음 개선 지점**: 판정 불가가 가장 큰 규칙은 **셋이 나란히** 있다 —
규칙 6(86.7%) · 12(85.7%) · 9(83.7%). 「규칙 6」만 적어 두면 나머지를 놓친다.
근거: `docs/research/opendb-schema-analysis.md` §9.3.2

- 6·9는 **케이스 데이터**다. `/admin`으로 채우면 줄어든다. 다만 제조사 주소가
  있는 것이 8.7%뿐이어서(§10.4) 나머지는 출처를 직접 찾아야 한다
- 12는 **메인보드 출시 연도**다 (79.5% 결측). 소켓 첫해 CPU는 소켓의 첫 CPU 연도를 하한으로
  판정한다 (이슈 #28, §12.5) — 같은 소켓 조합 판정 불가 83.2% → 67.8%. 나머지는 원본에 경로가 없다
- 17은 드라이브 1개면 91.9%를 판정한다 (이슈 #27). **2개 이상은 개수를 못 세서** 판정 불가다 —
  결측이 아니라 모순이라 채워서 줄지 않는다 (§17.4)

**규칙이 읽는 입력은 전부 `SPEC_REQUIREMENTS`에 선언한다.** 빠뜨리면 `/rules`가
결측을 축소해서 보여준다 — 규칙 12가 그랬다 (0.7%로 표시, 실제 85.7%).
`parts` 컬럼에 있는 값은 `storedOnPart: true`로 싣는다 (이슈 #15).
`apps/ingest/test/undeclared-input.test.ts`가 그 누락을 수치로 잡는다.

그때까지의 완화책으로, 판정 불가 3,196건 중 3,056건(95.6%)에 같은 폼팩터의
**관측된** 지원 분포를 참고로 덧붙였다 (ADR-0013). 판정은 여전히 `unknown`이다.
form_factor로 추론해 통과시키지 않는다 — Mini ITX Tower는 82%가 SFX 전용이다.
근거: `docs/research/case-psu-formfactor-distribution.md`

## 보류 중

`docs/pipeline-plan.md`는 자율 운영 인프라 설계다.
**지금은 구현하지 않는다.** 앱이 배포 가능한 상태가 된 뒤에 착수한다.
참고용으로만 읽는다.

## 기술 스택

확정: ADR-0010. 근거와 탈락 대안은 `docs/research/stack-requirements.md`.

| 구분 | 확정 |
|---|---|
| 프론트·백엔드 | **Next.js (TypeScript)** — App Router, SSR/ISR |
| DB | **PostgreSQL** — `part_specs`는 JSONB |
| 규칙 엔진 | 프레임워크 비의존 **순수 TS 모듈** |
| ~~정규화 워커 (Phase 2)~~ | 폐기 (ADR-0022). 스택은 TypeScript 하나다 |
| 캐시·큐 | 두지 않는다 |

- **규칙 엔진을 Next.js에 의존시키지 않는다.** DB 접근도 프레임워크 API도 쓰지 않는
  순수 함수로 두고, 클라이언트와 서버가 같은 코드를 쓴다. 판정 로직이 두 벌이 되면
  "편집 중엔 통과인데 공유 링크에선 오류"가 난다

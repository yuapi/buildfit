# 저장 포맷 버전별 샘플

`pc-builder-spec.md` §10.4가 요구하는 것이다.

> **localStorage 마이그레이션: 과거 모든 버전의 샘플 데이터를 보관하고, 배포마다
> 전 버전이 정상 복원되는지 검증**

**여기 있는 파일은 지우지 않는다.** 포맷 버전을 올릴 때마다 그 시점의 실제 저장
내용을 `v<N>.json`으로 추가한다. 파일 내용은 `localStorage`에 들어간 모습 그대로
(값이 문자열) 둔다.

`storage.test.ts`가 이 디렉터리를 전부 읽어 복원되는지 검증한다.
